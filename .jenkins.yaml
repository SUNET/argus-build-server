---
git:
  branch: main
triggers:
  cron: "@daily"
clean_workspace: true
pre_build_script:
- cp docker/Dockerfile .
script:
- echo "Build SUNET Custom ARGUS Image"
builders:
- docker
docker_name: sunet/argus-api-test
docker_tags:
- "v1.26.0_sunetbuild"
docker_build_args: |
  BRANCH_NAME="v1.26.0"
  REPO_URL="https://github.com/Uninett/Argus.git"
